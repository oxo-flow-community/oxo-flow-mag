# Environment for MetaEuk — pinned exactly to the upstream
# nf-core/mag 5.5.0 nf-core/metaeuk/easypredict module environment
# (modules/nf-core/metaeuk/easypredict/environment.yml); used by the
# MetaEuk branch. The database is user-provided or downloaded by mmseqs
# (config.metaeuk_db / config.metaeuk_mmseqs_db).
name: metaeuk
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::metaeuk=6.a5d39d9
