---
# Environment for Pypolca — the upstream nf-core/mag 5.5.0 pin
# (modules/nf-core/pypolca/run/environment.yml: bioconda::pypolca=0.4.0).
name: pypolca
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.10
  - bioconda::pypolca=0.4.0
