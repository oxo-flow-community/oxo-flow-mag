---
# Environment for mag_depths_summary — pinned exactly to the upstream
# nf-core/mag 5.5.0 upstream nf-core/mag mag_depths_summary local module environment (modules/local/mag_depths_summary/environment.yml).
name: mag_depths_summary
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::python=3.10.6
  - bioconda::pandas=1.4.3
