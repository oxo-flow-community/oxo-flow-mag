---
# Environment for Chopper — the upstream nf-core/mag 5.5.0 pin
# (modules/nf-core/chopper/environment.yml: bioconda::chopper=0.9.0).
name: chopper
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.10
  - bioconda::chopper=0.9.0
