---
# Environment for split_fasta — pinned exactly to the upstream
# nf-core/mag 5.5.0 upstream nf-core/mag split_fasta local module environment (modules/local/split_fasta/environment.yml).
name: split_fasta
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::metabat2=2.15
  - python=3.6.7
  - biopython=1.74
  - conda-forge::pandas=1.1.5
