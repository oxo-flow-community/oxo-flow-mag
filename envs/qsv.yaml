---
# Environment for qsv — pinned exactly to the upstream
# nf-core/mag 5.5.0 upstream nf-core/qsv/cat 5.1.0 module environment (modules/nf-core/qsv/cat/environment.yml).
name: qsv
channels:
  - conda-forge
  - bioconda
dependencies:
  - conda-forge::qsv=5.1.0
