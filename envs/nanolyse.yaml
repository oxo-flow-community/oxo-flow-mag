---
# Environment for NanoLyse — the upstream nf-core/mag 5.5.0 pin
# (modules/nf-core/nanolyse/environment.yml: bioconda::nanolyse=1.2.0).
name: nanolyse
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.10
  - bioconda::nanolyse=1.2.0
