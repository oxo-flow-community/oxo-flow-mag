---
# Environment for Porechop — the upstream nf-core/mag 5.5.0 pin
# (modules/nf-core/porechop/environment.yml: bioconda::porechop=0.2.4).
name: porechop
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.10
  - bioconda::porechop=0.2.4
